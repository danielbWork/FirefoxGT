import { Typography } from "@mui/material";
import React from "react";

/**
 * Ui displayed when group tab is invalid
 */
export const InvalidGroupTab = () => {
  return (
    <Typography variant="h1" color="WindowText" align="center">
      Invalid Group Tab
    </Typography>
  );
};
